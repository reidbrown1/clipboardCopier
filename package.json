{
  "name": "clipboardcopier",
  "version": "1.0.0",
  "description": "An Electron app that displays clipboard contents when a shortcut is pressed",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "electron",
    "clipboard"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^35.1.1",
    "electron-reload": "^2.0.0-alpha.1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  }
}
